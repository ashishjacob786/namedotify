generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model PageView {
  id          String   @id @default(uuid())
  visitorId   String   
  pageUrl     String   
  referrer    String?  
  eventType   String   @default("pageview") 
  eventData   String?  
  country     String?  
  city        String?  
  device      String?  
  browser     String?  
  os          String?  
  createdAt   DateTime @default(now())

  @@index([createdAt])
  @@index([pageUrl])
  @@index([visitorId])
}

// 🚀 Advanced Blog CMS Model
model BlogPost {
  id          String   @id @default(cuid())
  title       String
  slug        String   @unique // Permalink (e.g., /blog/my-post)
  content     String   // ✅ बस यहाँ से @db.Text हटा दिया है
  excerpt     String?  // Short description for SEO and Blog Card
  category    String   @default("General")
  tags        String   // Storing tags as comma-separated string
  author      String   @default("NameDotify Admin")
  status      String   @default("published") // 'draft' or 'published'
  featuredImg String?  // URL of the cover image
  imageAlt    String?  @default("Blog featured image") // ✅ New: Image SEO
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}