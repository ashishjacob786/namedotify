export const metadata = {
  title: 'Admin Dashboard | NameDotify',
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}